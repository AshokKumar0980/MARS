spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pipeline_db?useSSL=false&serverTimezone=UTC
    username: root
    password: password123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # (create, update, validate, none)
    show-sql: true

# Example API config
api:
  base-url: "https://api.example.com/v1/items"
  api-key: "your_api_key_here"

# Scheduler (every day at 2 AM)
scheduler:
  cron: "0 0 2 * * *"
